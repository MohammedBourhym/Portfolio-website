---
import Layout from "@/layouts/Layout.astro";
import projects from "@/data/projects";
import type { Project } from "@/data/projects";

---

<Layout title="All Projects">
  <main class="flex flex-col gap-20">
    <h1 class="text-2xl text-gray-900 dark:text-neutral-100">Projects</h1>

    <article class="flex flex-col gap-4">
      {
        projects.map((project: Project) => (
          <a
            class="group flex flex-col gap-2 border-t border-gray-300 dark:border-neutral-700 py-4 transition-all hover:text-gray-900 dark:hover:text-neutral-100"
            href={project.link}
            target="_blank"
            rel="noopener noreferrer"
          >
            <div class="flex w-full flex-col md:flex-row md:items-center md:justify-between">
              <h2 class="text-lg">{project.title}</h2>

              <div class="flex flex-row items-center gap-4">
                <p class="text-sm text-gray-600 dark:text-neutral-400">{project.techs.join(" • ")}</p>
                <svg
                  width="18"
                  height="18"
                  viewBox="0 0 18 18"
                  fill="none"
                  class="transition-all duration-300 group-hover:translate-x-1"
                >
                  <path
                    d="M5.25 12.75L12.75 5.25"
                    stroke="#999999"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  />
                  <path
                    d="M5.25 5.25H12.75V12.75"
                    stroke="#999999"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  />
                </svg>
              </div>
            </div>
            {project.description && (
              <p class="text-gray-600 dark:text-neutral-400">{project.description}</p>
            )}
          </a>
        ))
      }
    </article>
  </main>
</Layout>
